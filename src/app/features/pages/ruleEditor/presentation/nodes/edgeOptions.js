import { MarkerType } from "reactflow";

const edgeOptions = {
  markerEnd: {
    type: MarkerType.ArrowClosed,
  },
  type: "smoothstep",
  style: {
    stroke: "#a6a6a6",
  },
};

export default edgeOptions;
